AutotoolsPackage:   fftw

Description:
    FFTW is a C subroutine library for computing the discrete Fourier
    transform (DFT) in one or more dimensions, of arbitrary input size, and
    of both real and complex data (as well as of even/odd data, i.e. the
    discrete cosine/sine transforms or DCT/DST). We believe that FFTW, which
    is free software, should become the FFT library of choice for most
    applications.

Homepage: https://www.fftw.org

Preferred version:  
    3.3.11       https://www.fftw.org/fftw-3.3.11.tar.gz

Safe versions:  
    3.3.11       https://www.fftw.org/fftw-3.3.11.tar.gz
    3.3.10       https://www.fftw.org/fftw-3.3.10.tar.gz
    3.3.9        https://www.fftw.org/fftw-3.3.9.tar.gz
    3.3.8        https://www.fftw.org/fftw-3.3.8.tar.gz
    3.3.7        https://www.fftw.org/fftw-3.3.7.tar.gz
    3.3.6-pl2    https://www.fftw.org/fftw-3.3.6-pl2.tar.gz
    3.3.5        https://www.fftw.org/fftw-3.3.5.tar.gz
    3.3.4        https://www.fftw.org/fftw-3.3.4.tar.gz
    2.1.5        https://www.fftw.org/fftw-2.1.5.tar.gz

Deprecated versions:  
    None

Variants:
    build_system [autotools]        autotools
        Build systems supported by the package

    mpi [true]                      false, true
        Activate MPI support

    openmp [false]                  false, true
        Enable OpenMP support.

    pfft_patches [false]            false, true
        Add extra transpose functions for PFFT compatibility

    precision [float,double]        double, float, long_double, quad
        Build the selected floating-point precision libraries

    shared [true]                   false, true
        Build shared libraries


Dependencies:
    autoconf           build
      when +pfft_patches

    automake           build
      when +pfft_patches

    c                  build

    fortran            build

    gmake              build
      when build_system=autotools

    gnuconfig          build
      when build_system=autotools target=ppc64le:

    gnuconfig          build
      when build_system=autotools target=aarch64:

    gnuconfig          build
      when build_system=autotools target=riscv64:

    libtool            build
      when +pfft_patches

    llvm-openmp        build, link
      when +openmp %apple-clang

    mpi                build, link
      when +mpi


Licenses:
    GPL-2.0-or-later


